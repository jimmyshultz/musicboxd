# Build Script Phase for Production Environment
# Add this as a "Run Script Phase" in Xcode Build Phases
# Position: BEFORE "Compile Sources"
# Shell: /bin/sh

if [ "${CONFIGURATION}" = "Release" ]; then
  export ENVFILE=.env.production
  echo "✅ Using production environment: $ENVFILE"
else
  echo "ℹ️  Using default environment for configuration: $CONFIGURATION"
fi